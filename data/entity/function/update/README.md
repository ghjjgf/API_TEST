# Entity Update 设计用例

## 职责
保存实体 `update` 动作的输入定义。

## 目录内容
- `source_legacy_cases.json`：用例列表。

## 入口
当前只作为设计输入，不进入默认可执行测试。

## 输入与输出
- 输入：实体 payload 和校验规则。
- 输出：当前不生成运行结果。

## 依赖关系
依赖 `function_test/support.py` 和 `api/api.py`。
